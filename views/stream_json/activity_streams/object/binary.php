<?php
/**
 * AS binary object.
 *
 * @uses array  $vars['compression'] A list of compression algorithms used on the data.
 * @uses string $vars['data']        The base 64 encoded binary data.
 * @uses string $vars['file_url']    The IRI of the binary data
 * @uses int    $vars['length']      The total number of unencoded, uncompressed octets of the data.
 * @uses string $vars['md5']         The md5 checksum of the uncompresed, unencoded data.
 * @uses string $vars['mime_type']   The mime of
 *
 * @todo Implement or remove. This is sort of a stupid object.
 */
